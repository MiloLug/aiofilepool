[project]
name = "aiofilepool"
version = "0.1.0"
description = "Async file-descriptor pool: allows holding thousands of usable async file handles"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Lia Va" }]
keywords = [
    "asyncio",
    "async",
    "file",
    "io",
    "file-descriptor",
    "pool",
    "chunked",
    "concurrency",
]
classifiers = [
    "Framework :: AsyncIO",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Programming Language :: Python :: 3.15",
    "Topic :: Software Development :: Libraries",
    "Typing :: Typed",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/MiloLug/aiofilepool"
Repository = "https://github.com/MiloLug/aiofilepool"

[dependency-groups]
validation = [
    "pytest",
    "pytest-asyncio",
    "hypothesis>=6.115.0",
    "mypy>=1.19.1",
    "ruff>=0.15.12",
    "pytest-xdist>=3.8.0",
]
dev = [
    {include-group = "validation"},
    "pre-commit"
]

[build-system]
requires = ["uv_build>=0.12,<0.13"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "aiofilepool"
module-root = ""

[tool.pytest.ini_options]
asyncio_mode = "auto"
markers = [
    "slow: slow-running property/stress tests (deselect with '-m \"not slow\"')",
]
